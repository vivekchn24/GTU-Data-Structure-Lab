#include <stdio.h>
#include<math.h>
 void main()
 void merge_sort(int L,int R,int mid,int a[],int b[])
{
    int i,j,k;
    
    if (L<R)
    { 
        mid=(L-R)/2;
        merge_sort(0,mid);
        merge_sort(mid-1,n-1);
        combine(L,mid,R);
    }
    combine(L,mid,R);
    {
        i=L;
        j=mid-1;
        k=L;
        while(i<=mid&&j<=R)
        {
            if(a[i]<=a[j])
            {
                b[k]=a[i];
                i++,k++;
            }
            else
        {
            b[k]=a[j];
            j++,k++;
        }
        if(i>mid)for leftover in;
        {
            while(j<=R)
            {
                b[k]=a[j];
                j++,k++;
            }
        }
        elle] == search) {
      printf("%d found at location %d.\n", search, middle+1);
      break;
    }
    else
      last = middle - 1        {
            a[k]=b[k];
        }
}
}

                
    
          